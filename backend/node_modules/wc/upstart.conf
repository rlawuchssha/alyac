# Install:
#
# sudo cp upstart.conf /etc/init/wc.conf
# sudo start wc
#
# Remove:
#
# sudo rm /etc/init/wc.conf

description "wc.js server"
author "Andrew Pennebaker <andrew.pennebaker@gmail.com>"

start on startup
stop on shutdown

script
	export HOME="/root"
	exec node /srv/d_hactar/usr/src/node-wc/wc.js -s 2>&1
end script
